import { makeStyles } from '@material-ui/core/styles';

const UseStyles = makeStyles({
    table: {
      minWidth: 500,
    },
    title: {
        textAlign: "center",
        marginTop: "80px",
        marginBottom: "40px"
    }
});

export default UseStyles;